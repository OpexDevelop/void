[package]
name = "void-core"
version = "0.1.0"
edition = "2026"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
aes-gcm = "0.10"
rand = "0.8"
uuid = { version = "1", features = ["v4"] }
