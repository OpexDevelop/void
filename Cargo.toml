[workspace]
members = ["core", "plugin-crypto", "plugin-ntfy", "plugin-storage"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

[profile.plugin-release]
inherits = "release"
opt-level = "s"
strip = "symbols"
panic = "abort"
