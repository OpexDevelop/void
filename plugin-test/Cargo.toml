[package]
name = "plugin-test"
version = "0.1.0"
edition = "2021"

[lib]
# Обязательно указываем cdylib, чтобы Rust скомпилировал это как динамическую библиотеку (wasm)
crate-type = ["cdylib"]

[dependencies]
# Подключаем Extism для Плагина (PDK - Plugin Development Kit)
extism-pdk = "1.3.1"
