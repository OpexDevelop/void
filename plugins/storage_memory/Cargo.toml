[package]
name = "storage_memory"
version = "0.2.0-alpha"
edition = "2021"

[lib]
name = "storage_memory"
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
